<%= form_with(model: role) do |form| %>
  <% if role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
        <% role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-outline mb-3">
    <%= form.text_field :name, autofocus: true, placeholder: "Name", class:"form-control form-control-lg" %>
    </div>
  <%= form.submit "Submit", class:"btn btn-primary btn-lg btn-rounded text-white px-5" %>
                  
<% end %>
